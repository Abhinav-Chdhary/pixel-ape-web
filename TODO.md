Easy:
[X] allow adding reference image in one half of the canvas and automatically drawing a grid over it. (the same size grid as the user is working with)
[X] allow selecting parts of the image and moving them around
[X] allow going to complete filelist somewhere in the navbar, deleting files, renaming them, duplicate them
[X] on new tab add options to 1. choose from existing files in the popup 2. open full filelist page in the new tab
[X] make default canvas size 32x32
[X] allow changing order of tabs
[X] allow closing tabs
[X] make tabs collapse in width when there are many tabs. just like it happens in chrome browser
[X] make changing canvas size smoother. right now if i delete current type 1 and 6. it gets typed as 46 for some reason
[X] no matter what the size the of canvas. it would always have sqaure grid lines. right now if grid size is 16x46. then it makes cells appear vertically long.
[X] increase upper limit to 512x512 and also make the user aware of this limitation when they create a new canvas

Medium:
[X] remove agent guide, might not be needed here.
[X] add color selection above 'foreground'/'background' section
[X] allow writing/editing hex code in color selection dialog
[X] an empty slot when selected, doesn't reflect correctly in palette's selected color area like 'foreground'. foreground shows some previously selected color

Hard:
[X] plan db schema and apis for user accounts, sprites, palettes
[ ] import of pallete packages
[ ] choosing default color pallete among the existing ones
[ ] adding more tools?
[ ] think of a cool ai feature (good to have)

Very hard:
[ ] collaborative, real time editing
[ ] agentic editing
[ ] exporting as game assets
[ ] showing thumbnails of files in list