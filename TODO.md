[ ] allow adding reference image in part of the canvas and automatically drawing a grid over it
[ ] allow selecting parts of the image and moving them around
[ ] allow going to filelist, deleting them, renaming them, duplicate them, close tabs options, 
[ ] allow changing order of tabs
[ ] allow closing tabs
[ ] make tabs collapse in width when there are many tabs. just like it happens in chrome browser
[ ] make changing canvas size smoother. right now if i delete current type 1 and 6. it gets typed as 46 for some reason
[ ] no matter what the size the of canvas. it would always have sqaure grid lines. right now if grid size is 16x46. then it makes cells appear vertically long.
[ ] increase upper limit to 512x512 and also make the user aware of this limitation
[ ] update the agent guide if tab name is changed. Tab name should match file name
[ ] add color selection above 'foreground'/'background' section
[ ] allow writing/editing hex code in color selection dialog
[ ] an empty slot when selected, doesn't reflect correctly in palette's selected color area like 'foreground'
[ ] provision such that human and ai can programmatically add palette colors
[ ] import of pallete packages
[ ] choosing default color pallete among the existing ones