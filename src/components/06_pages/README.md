# Pages

`App.tsx` is currently the Pixel Editor page. It keeps editor state, canvas drawing,
history, and persistence together while visual UI is extracted into the lower atomic
layers. Move it here only when a second route or page is introduced.
